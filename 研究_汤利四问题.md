# 汤利测试集四问题研究（审阅稿）

> 排查基准：`output/汤利`（玩家 麦克·汤利 id=38701，867–890，11 份快照出第 1 个十年传记、21 份快照出第 2 个十年传记），日志 `logs/prompts.log` / `logs/journal.log`，代码 `facts.py / biography.py / cache_lib.py`，数据 `output/汤利/data/player_38701.json` 与 `melt_*.json`。
> 每条结论带代码行号或数据证据。新增提示词遵循「正向表述」铁律（no-negative-prompts），文件一律 UTF-8。
> **状态：研究稿，未动工。** 四个问题的修复方案见各节末尾，实施顺序见第六节。

---

## 问题 1：主角谋杀情人长女、继而私生女成为王国继承者——模型完全没捕捉到（核心）

### 1.1 数据层的事实还原（完整戏剧弧，全部可被程序算出）

逐档核对 `melt_*.json` 的 `k_asturias` 头衔记录（`landed_titles.database`）：

| 时间 | 事实 | 证据 |
|---|---|---|
| 867.2.19 | 阿尔东萨（14963）与阿方苏·奥多尼乌斯（15389）成婚 | 14963 记忆 `married` |
| 868.10.11 | 长女努妮娅（45135）出生 | 14963 记忆 `first_born` |
| 871.1.1 / 872.1.1 | **努妮娅（45135）是阿斯图里亚斯王位第一继承人** | melt_871/872：`k_asturias heir[0]=45135`，holder=15389 |
| 872.2.4–2.5 | 麦克·汤利与阿尔东萨（阿斯图里亚斯王后）私通、相恋 | 38701 记忆 `had_sex`(872.2.4) + `became_lovers`(872.2.5) |
| 872.5.29 | **麦克·汤利谋杀努妮娅（45135）**——王位继承人身亡 | 38701 记忆 `successful_murder victim=45135`；45135 死亡记录 `reason=death_mysterious, killer=38701` |
| 873.9.16 | 阿尔东萨诞下努妮娅·坎塔布里亚（16807251），**实为麦克·汤利之女** | 16807251 `family.real_father=[38701]`，法理父=15389 |
| 874.1.1 起 | **努妮娅·坎塔布里亚成为阿斯图里亚斯王位第一继承人** | melt_874/878：`k_asturias heir[0]=16807251` |

完整戏剧弧：**汤利与阿斯图里亚斯王后私通 → 谋杀王位第一继承人（王后与国王的长女）→ 一年后王后诞下实为汤利之女的次女 → 次女成为阿斯图里亚斯王位继承人——汤利的血脉将坐上阿斯图里亚斯王位，而国王对此一无所知。**

### 1.2 断链分析：这出戏为什么完全没进提示词（5 个断链）

#### 断链①：`successful_murder` 记忆没有渲染模板，被时间线静默丢弃（最致命）

- `facts.py:92–150` `MEMORY_TEMPLATES` 共 57 个类型，**没有 `successful_murder`**；
- `_mem_sentence`（facts.py:1882–1910）查无模板返回 `None`；
- `_timeline`（facts.py:2146–2149）的「其余记忆」分支 `if not s: continue`——**谋杀记忆在入口就被丢掉**；
- 第一十年汤利共杀 5 人（12788 / 45135 / 10862 / 46962 / 11797，38701 记忆），全部无声消失；而刺客列传阈值是「杀 >5 人」（biography.py:1274），第一十年不生成刺客列传 → **第一十年提示词里谋杀一次都不出现**。实测 `prompts.log` 55213–55473 区段（第 1 个十年全部请求）搜「谋杀/努妮娅」零命中。

#### 断链②：情人、受害者、私生女都不在「相关角色集」，无档案、无时间线存在感

- `_related_ids`（facts.py:2010–2058）级别 2 只收 父母/妻妾/子女/孙辈/儿媳女婿 + 妻妾父母；
- 情人 14963、受害者 45135、私生女 16807251 **全不在集内** → `_profile_needed_ids`（facts.py:2534）不给他们建档案；
- 受害者 45135 的死亡记录虽有 `killer=38701` 且 `_death_sentence`（facts.py:1940–1962）支持渲染「被谋杀，凶手为麦克·汤利」，但**死亡记录也只对相关集内角色渲染**（facts.py:2105–2109）→ 同样被丢。

#### 断链③：情人的身份链条（王后/丈夫/子女）无处渲染

- 阿尔东萨只以名字出现在两行记忆里：「872年2月4日，麦克·汤利与阿尔东萨·拉米鲁斯有私情。」「872年2月5日，麦克·汤利与阿尔东萨·拉米鲁斯相恋。」（prompts.log 55213–55214）；
- 「阿尔东萨·拉米鲁斯」是父名制渲染（父 9387 拉米鲁 + 后缀「斯」，cache_lib.py `_patronym_of`），模型无从知道她就是阿斯图里亚斯王后；
- 玩家行迹虽显示「872年1月1日驻阿斯图里亚斯-德奥维耶多伯爵领，阿方苏·奥多尼乌斯执掌，最高领主阿方苏·奥多尼乌斯」（prompts.log 56217），但**行迹里的「阿方苏·奥多尼乌斯」与情人记忆里的「阿尔东萨·拉米鲁斯」之间没有任何程序提供的连接**——让模型自己跨行联想两个名字，等于让它猜。

#### 断链④：继承者事实程序完全没读

- 熔件 `k_asturias.heir` 列表（含 16807251 为第一继承人）**程序从未读取**——全项目 grep `heir` 零命中；
- 16807251 的 `real_father=[38701]` 在缓存里有（v5 已支持「实父」渲染），但她不在任何档案里，`real_father` 字段无从渲染。

#### 断链⑤：第二十年刺客列传虽列出受害者，但关系链仍断

- 第二十年刺客列传 prompt 有「死者：努妮娅·阿方苏斯（殁于872年5月29日，死于一场未曾败露的谋杀，凶手为麦克·汤利。）」（prompts.log 56098），受害者档案含「父阿方苏、母阿尔东萨」（biography.py `_assassin_kill_lines` 亲缘行）；
- 但母亲显示为裸名「阿尔东萨」、与情人「阿尔东萨·拉米鲁斯」名字不一致；且 23 名死者中该条只是孤立一行，模型产出的猜测是「妇人、仇雠、或碍事之人？……史笔不见」（第 2 个十年 md 刺客列传），**无法连到王后与继承者**。

### 1.3 修复方案（按实施顺序，均符合「程序直算」原则）

#### 修复 A：补 `successful_murder` 渲染模板（必做，基础）

- `MEMORY_TEMPLATES` 增加 `"successful_murder": "{name}谋杀{other}。"`；
- `_timeline` 把 `successful_murder` 并入死亡去重分支（facts.py:2118 按死者 id 去重）：**受害者死亡记录存在时用死亡句**（含「死于一场未曾败露的谋杀，凶手为麦克·汤利」的悬念味，facts.py:88 已有 `death_mysterious` 雅化），**否则用谋杀句**；同一死者只留一条；
- 过滤：仅渲染「记忆拥有者为主角」的 `successful_murder`（路人谋杀记忆不进时间线）；
- `DRAMATIC_TIMELINE_TYPES`（facts.py:1129）加入 `successful_murder`，让谋杀进入【戏剧性事件】高亮。

#### 修复 B：程序直算「情人—受害者—继承者」关系链（核心戏剧）

新增 `facts.py` 函数（如 `_drama_chains(f)`），数据全由程序直算，生成自然语言戏剧块，注入共享前缀或本纪相关事实：

1. 取主角情人集（`became_lovers` / `had_sex` 参与者，排除妻妾）；
2. 对每位情人：取配偶（family.spouse）、配偶头衔（熔件 holder → k_/e_/h_ 头衔 → 「阿斯图里亚斯王后」类身份句）、共同子女；
3. 对每个子女：若死亡记录 `killer==主角` → 「X年X月X日，麦克·汤利谋杀其长女/之子+头衔（王位继承人）」；
4. 若子女 `real_father==主角` → 「实为麦克·汤利之女」；
5. 查熔件 `landed_titles[].heir`：若该子女为某王国/帝国头衔第一继承人 → 「自X年起为阿斯图里亚斯王位第一继承人」。

产出示例（程序拼接，模型不改写）：
```
阿尔东萨·拉米鲁斯为阿斯图里亚斯王后（夫阿方苏·奥多尼乌斯），872年2月与麦克·汤利私通相恋。
872年5月29日，麦克·汤利谋杀其长女努妮娅·阿方苏斯——当时阿斯图里亚斯王位第一继承人。
873年9月16日，阿尔东萨诞下努妮娅·坎塔布里亚，实为麦克·汤利之女；自874年起为阿斯图里亚斯王位第一继承人。
```

#### 修复 C：情人/受害者补档案与身份

- `_profile_needed_ids` 追加：情人、谋杀受害者（successful_murder victim）、`real_father==主角` 的角色；
- 受害者档案渲染 父/母（含「母：阿尔东萨（阿斯图里亚斯王后，麦克·汤利情人）」式注解——注解由程序按 B 的图谱直算，不让模型猜）；
- 情人档案渲染「配偶：阿方苏·奥多尼乌斯（阿斯图里亚斯国王）」——配偶头衔从熔件读（`_lt[tid].holder` + 婚配关系），解决断链③。

#### 修复 D（可选加分）：继承变更独立成块

若 B 的产出过于局部，可另建「继承风云」数据块：扫描相关角色的法理子女/私生子女是否出现在熔件高位头衔（k_/e_/h_）heir 列表首位，生成「X殁后，Y成为Z王位继承人（实为主角之女/子）」类条目，并入【戏剧性事件】。

---

## 问题 2：历史传输太流水账——自然语言化与程序直算

### 2.1 现状量化（第 1 个十年 prompt 实测）

- 【主角大事摘要】21 行（prompts.log 56339 起）：其中 **10 行是「结怨/结仇」重复动词**（870年4月一次3人、872/873/874 各一次），21 行全部是「X年X月X日，谁与谁+动词」平铺；
- 【人物档案·戏剧性事件】**重复同样的 9 条结怨**（prompts.log 56338），与【主角大事摘要】逐字重复；
- 【大事年表】61 行（本纪，prompts.log 56318 起），含大量「查理·加洛林的亲属X亡故」「X助盟友作战」式流水；
- 成对冗余：「872年2月4日，麦克·汤利与阿尔东萨·拉米鲁斯有私情。」+「872年2月5日……相恋。」两条只差一天，各占一行。

### 2.2 修复方案（程序聚合，模型只接收结果）

1. **同型聚合**：时间线渲染前按（类型, 年, 月）聚合同型事件——`870年4月，莱昂、昂盖朗、诺贝特三人先后与麦克·汤利结怨。`（现有 `_merge_same_day_events` facts.py:2239 只合「同日」，扩展到「同年/同月」档；人名 ≤3 全列，>3 收「等N人」）；「私情+相恋」同月合并为 `872年2月，麦克·汤利与阿尔东萨·拉米鲁斯私通相恋。`
2. **统计摘要行（程序直算）**：本纪事实块开头加程序算出的汇总——`本十年结怨9人、结仇1人、添丁6人、谋杀5人、私通2人。` 给模型数据锚点，省去逐条点数。
3. **去重**：【戏剧性事件】不再重复时间线主角事件（既然【主角大事摘要】已全量含主角事件，戏剧性事件块只留「短命皇朝」等非时间线事实，facts.py:1138 的②改为只收非时间线事实，或整体并入摘要）。
4. **关系与间隔直算**：程序算好模型现在自己数的东西——「874年5月4日结仇，875年1月1日身亡，仅隔八月」（第 2 个十年 md 里模型自己算出了这句，属于白耗注意力）、「长子殁于877年8月5日，年不足四岁」等，直接由程序产出。
5. **保留精确日期**：关键事件（婚/丧/子/仇/战/登位/囚狱）保留完整日期；琐碎同型事件（结怨、助战、亲属亡故）聚合到年/月粒度。

效果预估：第 1 个十年单请求 21 行摘要 → 约 12–14 行（去 10 行结怨为 3–4 行 + 1 行统计），【戏剧性事件】9 行消除；61 行年表 → 约 45–50 行。词元省 15–25%，且模型注意力从「数日期」转向「写戏剧」。

---

## 问题 3：相关人物档案太表格——自然语言化

### 3.1 现状

`biography.py _profile_lines`（367–464）输出逐行字段：

```
姓名：崔佛·菲利普
官职：瑞典国王
家族：菲利普
家训：麦克·汤利他妈的在哪？
生于846年1月1日
族属：诺斯人
信仰：阿萨神族
```

### 3.2 修复方案（程序端模板，干净事实铁律不变）

`_profile_lines` 改为按自然语言模板拼接（全部字段程序直出，**不让模型改写**——否则破坏「非虚构铁律」）：

- **首句（官职+名）**：`瑞典国王崔佛·菲利普`——有官职时官职前置（与刺客列传「唐皇帝李漼」同法），无官职直接用姓名；
- **身份句**：`菲利普氏，诺斯人，信阿萨神族，生于846年1月1日，家训「麦克·汤利他妈的在哪？」`（家族/族属/信仰/出生/家训按有无拼接，逗号连接）；
- **性情句**：`为人野心勃勃、狡诈、勤勉。` + `特质履历：野心勃勃（至晚自868年起已具）；……`（见问题 4 年化）；
- **政体/营地句**：`政体冒险者，营好麦坞帮，营规掠夺者/营地继承制/男性优先，营力310，现驻拜占庭伯爵领（巴西尔·马其顿执掌，最高领主东罗马帝国）。`
- **家庭句**：`妻室吉塞勒·加洛林，妾阿拉贡特/玛格丽特/埃尔夫吉富，子女崔西·麦克斯多蒂尔等6人，先世无考。`
- **任历/现状句**：`867年1月1日任好麦坞帮之主；年36岁，健康4.8，国库金93.6，威望991.2。`
- 句子以句号分隔，字段缺失则整句省略，顺序固定——保持程序确定性，只改「排版」不改「事实」。

同一函数是全部文章档案的单一出口（共享前缀/家室列传/好友仇人/朝局/妻族共用），改动集中、回归面小。

---

## 问题 4：特质履历日期只保留年

### 4.1 现状

`facts.py trait_history_lines`（1641–1668）用 `self.date()` 渲染完整日期，快照差分全是 1月1日：

```
患病（自871年1月1日起获得，自872年1月1日后消失；自876年1月1日起获得，自877年1月1日后消失）
```

日粒度无意义且占词元。

### 4.2 修复方案

`trait_history_lines` 内将日期格式化为年：`自871年起获得` / `自872年后消失` / `至晚自868年起已具`（`first: true` 的同理年化）。改动仅此一处，记忆类事件（如 872.5.29，游戏精确日期）不受影响。

---

## 问题 5（补）：教宗被渲染成「教宗国国王主教戈德弗鲁瓦」

### 5.1 现象

刺客列传死者条目：`教宗国国王主教戈德弗鲁瓦（殁于876年11月6日…）`。玩家所见游戏内该角色为「教宗」。

### 5.2 根因

- 戈德弗鲁瓦（46962）持有 `k_papal_state`（107，教宗国）——该头衔正是其信仰（faith 30，christianity_religion）的 `religious_head` 宗教领袖头衔，即**教宗本人**（title history：870.1.20–876.11.6，876.11.6 起由 16826930 继任）；
- 但 `official_title` 走通用神权官职词：`dead_data.flavor=king_theocracy_male_christianity_religion` → 本地化「国王主教」，地名补全「教宗国」→「教宗国国王主教」；
- 「国王主教」是**非领袖神权君主**的通用官职词（Prince-Bishop），教宗本人应使用宗教领袖称谓 `religiousheadname_pope` → 「教宗」。

### 5.3 修复（facts.py `religious_head_word`）

- 新增 `religious_head_word(cid)`：角色为其信仰的 `religious_head` 头衔的**现任或曾任持有者**（title history 判定，`_hold_intervals` 已按 as_of 截断，防十年传记泄漏后期继任教宗）且头衔为教皇座类（`papal`/`papacy`）时，返回「教宗」；
- `official_title` 与 `_feud_role_title` 优先调用该称谓；
- 其余宗教领袖（哈里发等）仍走既有 `realm_name` 动态国名路径，不套用「教宗」。

### 5.4 附带修复：熔件重复键合并（阴谋参与者提取的基础）

- 熔件 JSON 中 `agent_slots`（阴谋参与者槽）被 rakaly 渲染成**重复键**，`json.load` 只留最后一个 → 阴谋参与者全部丢失；
- `cache_lib.load_melt` 增加 `object_pairs_hook=_merge_dup_pairs`：重复键合并为列表（agent_slots / family_data.spouse / temporary_opinion / variables.item 等）；
- 连锁适配：`Facts.__init__` 头衔历史解析与 `_realm_facts` 支持「同日多事件（destroyed+created）」列表值。

---

## 问题 6（补）：关系原因 — 游戏自带 `opinions.active_opinions` 直接读取

### 6.1 需求

模型此前只知道「X年X月，A与B结仇」，不知道**为何**结仇。用户示例：拜占庭皇女阿纳斯塔西娅因**亲人被主角谋杀**才与麦克成为仇敌——这个原因游戏自己就记录着。

### 6.2 数据源（熔件 `opinions.active_opinions`）

每对角色一条记录，`scripted_relations` 带 `reason` 键（本地化 542/542 全命中）：

| 角色对 | scripted_relations | 中文 |
|---|---|---|
| 阿纳斯塔西娅→主角 | `rival: rival_murderer` | 麦克·汤利杀害了阿纳斯塔西娅·马其顿的亲近之人。 |
| 莱昂等→主角 | `grudge: rival_robbed_me` | 麦克·汤利在路上抢劫了莱昂。 |
| 吉塞勒↔主角 | `lover: lover_letter` | 麦克·汤利寄出写得很好的情书，以此勾引了吉塞勒·加洛林。 |
| 萨洛蒙→主角 | `rival: rival_slept_with_spouse_corresponding` | （其妻被勾引） |

阿纳斯塔西娅侧还带 `temporary_opinion: murdered_close_family_crime`（878.8.1 起 -80 好感，正是其父巴西尔·马其顿被杀日），全家 7 个兄弟姐妹同仇。

### 6.3 实现

- **localization.py**：`relation_template()` 保留角色名标签（`[CHARACTER.GetShortUIName]` 等 10 类），其余格式码照旧剥离；`build` 时把含角色名标签的键另存 `relation_templates`（10,699 条）→ JSON schema 2（向后兼容 schema 1）；
- **facts.py `Facts.relation_reasons(cid, kinds)`**：惰性索引 `opinions.active_opinions` 中主角相关对 → 按关系类型（rival/grudge/friend/lover…）取 reason → 模板替换名字（`TARGET_CHARACTER_2` 用 `involved_character`）→ 去重；**时间门**：该关系需有 ≤ as_of 的记忆（十年传记不泄漏 as_of 后形成的关系）；
- **biography.py**：`结仇缘由` = 游戏原因 + 程序直算因由（亲属被谋杀/配偶私通/托卵，补死者被剪除 opinions 的缺口）+ 结怨日期句；`结友缘由` 同理优先游戏原因。
- 注意：旧版 localization.json（schema 1，无模板）时该功能自动降级为程序直算因由。
- 局限：opinions 是**当前快照**的活动关系——关系人死亡/解除后条目被剪除（实测布林约尔夫 891 结友、900 死后 892 档的 `friend_dinner` 原因即消失），此时降级为记忆句+程序因由；活着的仇人/好友（阿纳斯塔西娅等）取到游戏原因。

### 6.4 好友列传删减

`build_articles`：无真好友（`_pick_friend` 走同朝共事者兜底）时**直接删去《列传·好友》**——兜底代打只是复述主角故事（汤利/菲利普4/冯·大马士革/边氏均删）；无仇人时同理删《列传·仇人》。文章数与总纲预告自动联动。

---

## 五、风险与边界

| 项 | 说明 |
|---|---|
| 修复 A 的时间线膨胀 | 杀人多的主角（汤利第二十年杀 23 人）时间线会因谋杀行变长——谋杀视为主角级事件参与截断优先（facts.py:2186 已有 lvl1 全保留机制）；刺客列传已在册的死者靠死亡去重避免双列 |
| 修复 B 的产出范围 | 只扫「情人家庭」关系链，不扫全图，避免噪声；产出块限 3–5 行 |
| 修复 C 的 token 成本 | 情人/受害者档案按精简版渲染（只给身份/亲缘/死因），不给全量特质履历 |
| 修复 D 的继承数据 | `heir` 是快照时点的继承列表，十年传记按 as_of 档取；只在高位头衔（k_/e_/h_）且 heir[0] 与主角有血缘/情人关系时渲染 |
| 提示词铁律 | 所有新增提示词/事实句用正向表述，无「不要/避免」类措辞 |

## 六、实施顺序建议

1. **问题 4（1 处改动，零风险）**：`trait_history_lines` 年化；
2. **问题 1 修复 A（模板+去重，风险小）**：`successful_murder` 进时间线与戏剧性事件；
3. **问题 3（单出口排版改造）**：`_profile_lines` 自然语言化；
4. **问题 2（聚合+统计+去重）**：同型聚合、统计摘要行、戏剧性事件去重；
5. **问题 1 修复 B/C/D（新数据链路，工作量最大）**：关系链直算、情人/受害者档案、继承变更块。

验证方式：以 `output/汤利` 缓存重跑十年传记（`pipeline.py bio 38701` 或重跑十年生成），对照 prompts.log 与生成文本，检查：① 谋杀进入时间线；② 关系链块出现且正确；③ 档案为自然语言；④ 特质只有年；⑤ 词元下降。
