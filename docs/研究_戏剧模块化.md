# 戏剧模块化可行性研究：十年小传按 30 个戏剧性模块切片传输

> 研究基准：`output/菲利普2`（玩家 崔佛·菲利普 id=38696，868–911，4 篇十年小传 + 终传），日志 `logs/prompts.log`，代码 `biography.py / facts.py / pipeline.py`，修复框架 `修复方案_菲利普2.md`（问题 4）。
> 本文件回答：把生成请求拆成 **30 个戏剧性模块**，每次生成十年小传时除总纲与刺客列传外只抽取该十年戏剧主题相关的事实喂给模型，能否显著减少词元消耗并增强叙事效果。
> 全部结论带实测数据（prompts.log 解析 + facts.py 原型跑分）。新增提示词遵循「正向表述」铁律（no-negative-prompts），文件 UTF-8。
> **状态：v14 已实施**（修复方案_菲利普2.md 全量 + 30 模块切片 + 刺客列传官职/亲缘/婚恋口径），实施记录见第九节。

---

## 一、结论摘要

| 判项 | 结论 |
|---|---|
| 技术可行性 | **成立**。时间线事件全部带 `type` 字段，type→模块 是纯数据层映射，原型实测 **58/58 类型 100% 归类、零未分类**；十年主题检测只用计数排序，不依赖 LLM。 |
| 词元节约 | **已实施，实测 −68%**（313,432 → 100,482 字符，第 4 个十年 16 请求）。详见第八节「实施记录」。 |
| 叙事增强 | **方向成立**，与 问题 4 的 D:\Journal 对照结论一致：D:\Journal 按板块切片（中位 2,236 字符）戏剧性明显强于本项目全量重发（中位 13,065 字符）。切片把模型注意力从 405 条宗族噪音上收拢到本十年的戏剧主线上。 |
| 前置依赖 | 依赖 修复方案_菲利普2.md 问题 4 修复第 1 条（年表分级限量）打底；模块表本身可与该方案并行实现。 |
| 主要风险 | 信息缺失（跨模块关联事件被裁）、并发文章口径不一致。均有对策，见第七节。 |
| 总体判定 | **已实施**（修复方案_菲利普2.md 全量 + 30 模块切片）。用户新开档 `--force` 重跑对照验证即可，见第八节实施记录。 |

---

## 二、现状量化（问题 4 的延续）

### 2.1 十年小传的请求解剖（prompts.log 实测）

第 4 个十年（898–908，生成于 2026-08-29 23:16，`崔佛·菲利普(846)_传记_第4个十年_908_01_01.md`）：

| 阶段 | 请求数 | 字符范围 | 合计字符 |
|---|---|---|---|
| 总纲 | 1 | 13,011 | 13,011 |
| 各篇开篇 (lead) | 7 | 16,019–26,827 | 136,914 |
| 各篇纪事 (mid) | 8 | 15,976–26,622 | 163,507 |
| **合计** | **16** | — | **313,432** |

单请求解剖（以「本纪·纪事」26,622 字符为例）：
- 【主角大事年表】独占 **约 25K 字符 / 占 90%+**，且与其余 15 个请求**逐字节重复**；
- 年表 455 条事件里只有 **50 条（11%）含主角名**（1,343 字符），**405 条（89%）是宗族/姻亲噪音**（9,767 字符）；
- 主角自己的戏剧（899 得瑞典、901 处决唐氏、903 劫掠范承宗）淹没在 `relative_died×54、released_from_prison×39、joined_allys_war×31、childhood_education×25` 等远亲琐事里。

### 2.2 重复成本量化

- 共享前缀（传主+档案+**全量年表**）经 `_shared_facts_block`（biography.py:698–720）注入**每一次**调用；
- 第 4 个十年：年表约 11.6K 字符 × 16 请求 ≈ **185K 字符 / 占整场 59% 是同一份年表的重复搬运**；
- 第 1 个十年（21:34 那场）同样：13 请求 / 89,779 字符，年表重复约 2.8K×13 ≈ 36K。

### 2.3 与 D:\Journal 的差距（问题 4 已证）

| 项目 | 请求数 | 中位 | P90 | 最大 |
|---|---|---|---|---|
| 本项目 Roman | 80 | 13,065 字符 | 27,130 | 43,489 |
| D:\Journal | 866 | 2,236 | 3,233 | 10,886 |

D:\Journal 的戏剧性强于本项目，其结构性优势正是「按板块只传该板块数据 + 每板块明确的叙事要求」——本研究的 30 模块切片把这一优势推向极致：**不止按文章切片，还按戏剧主题切片**。

---

## 三、方案设计：30 个戏剧性模块

### 3.1 模块表（30 个，含 26 个时间线模块 + 4 个专题模块）

每个模块 = {模块名, 数据源（记忆 type 集 / 专题渲染器）, 适用文章}。**type→模块 为纯数据层映射**（facts.py 已有 57 个 MEMORY_TEMPLATES 键，原型验证全覆盖）：

| # | 模块 | 数据源（memory type） | 适用文章 |
|---|---|---|---|
| 1 | 起家发迹 | `ascended_throne_memory` | 本纪/朝局 |
| 2 | 失位让土 | `lost_title_memory` | 本纪/朝局 |
| 3 | 开战兴兵 | `offensive_war, defensive_war, joined_allys_war` | 本纪/朝局 |
| 4 | 战和胜负 | `battle_won/lost_memory, war_won/lost` | 本纪/朝局 |
| 5 | 战殁负伤 | `witnessed_death_battle, became_incapable_due_to_battle_concussion` | 本纪/朝局 |
| 6 | 人质质任 | `hostage_created_*` (3 型) | 本纪/朝局 |
| 7 | 囚禁入狱 | `imprisoned, imprisoned_other` | 本纪/朝局 |
| 8 | 获释出狱 | `released_from_prison_memory` | 本纪/朝局 |
| 9 | 刑虐残暴 | `tortured_memory, torturer_memory` | 本纪 |
| 10 | 受辱含冤 | `ignored_assault_memory` | 本纪 |
| 11 | 结仇结怨 | `became_rivals, became_grudge` | 仇人/朝局 |
| 12 | 死敌之仇 | `became_nemesis` | 仇人/朝局 |
| 13 | 化仇解怨 | `stopped_being_rivals` | 仇人/朝局 |
| 14 | 仇雠消亡 | `rival_died` | 仇人 |
| 15 | 结友知交 | `became_friends` | 好友 |
| 16 | 挚友血盟 | `became_soulmates, became_blood_brother` | 好友 |
| 17 | 丧友之恸 | `friend_died` | 好友/家室 |
| 18 | 婚配联姻 | `married, grand_wedding_completed_guest` | 家室 |
| 19 | 情变私通 | `became_lovers, had_sex, broke_up_lovers` | 家室 |
| 20 | 丧偶之痛 | `spouse_died` | 家室 |
| 21 | 添丁进口 | `child_born, first_born, twins_born` | 家室 |
| 22 | 幼殇夭折 | `child_premature, child_stillborn` | 家室 |
| 23 | 丧亲之恸 | `relative_died` | 家室/本纪 |
| 24 | 教化求学 | `childhood_education_* (2 型), ward_education_completed, completed_rites_of_passage, completed_adult_education` | 家室/本纪 |
| 25 | 科考功名 | `passed/failed_*_exam_memory` (6 型) | 家室/本纪 |
| 26 | 拥戴加冕 | `became_acclaimed, witnessed_a_coronation_memory` | 本纪/朝局 |
| 27 | 信仰皈依 | `completed_hajj_memory, picked_serenity/creation_aspect_memory` | 本纪 |
| 28 | 死亡谢幕 | `death`（死亡记录，按死者关系并入 14/17/20/23 同键去重） | 本纪/家室 |
| 29 | 短命皇朝 | 专题渲染器 `dramatic_facts`（facts.py:1024，≤30 日在位即失） | 本纪/朝局 |
| 30 | 天下更替 | 专题渲染器 `realm.holder_changes`（facts.py:2389）+ `court_position_changes` | 朝局 |

**文章级整块数据源**（不进时间线切片，随各自文章整块携带，均为专用渲染器）：`killed`(刺客列传·刀下诸魂，用户明确豁免)、`house_feuds`(家族恩怨录)、`family_artifacts`(宝物志)、`wandering`(游侠列传)、`luminaries`(群英录)、`imperial_spouses`(妻族传)、`genealogy`(世系)。

### 3.2 十年主题检测（纯计数，无 LLM）

对第 N 个十年窗口 [起始年+(N-1)×10, 起始年+N×10) 内的时间线事件按模块计数排序：

```
module_score(m) = Σ 事件权重（主角参与 ×3，家人参与 ×2，路人 ×0，仅统计相关集内事件）
十年戏剧主题 = 得分 Top2–3 且 ≥ 阈值（如 ≥3 条）的模块
```

第 4 个十年（898–908）原型实测：`囚禁入狱 26、丧亲之恸 18、教化求学 16、添丁进口 10`——主题检测会把「囚狱+丧亡」判定为本十年主线，与正文实际走向吻合（该十年崔佛大兴囚狱）。

### 3.3 文章→模块 切片口径

`_article_facts`（biography.py:482）改为按文章取**模块切片**而非全量年表：

| 文章 | 事实切片（时间线模块） | 整块数据源 |
|---|---|---|
| 本纪 | 1–10, 23–29（主角级事件 + 权力/兵戈/刑狱模块） | 人物档案、历任、短命皇朝 |
| 列传·好友 | 15–17 + 结友缘由 | 传主档案、传主行迹 |
| 列传·仇人 | 11–14 + 结仇缘由 | 传主档案、传主行迹 |
| 家室列传 | 17–25, 28 | 家室档案（家人档案+事件） |
| 朝局风云录 | 1–8, 11–13, 26, 30 | 天下大势、朝中要员、官职任免 |
| 家族恩怨录 | — | house_feuds 整块 |
| 宝物志 | — | family_artifacts 整块 |
| 刺客列传 | — | killed 整块（按十年死亡日期切片，见 7.3） |

### 3.4 共享前缀与总纲

- 共享前缀瘦身为【传主】+【人物档案】+【十年戏剧主题预告】+ 主角级事件摘要（**主角名在文本中的事件，全量保留——第 4 个十年仅 50 条/1.3K 字符**），仍逐字节一致置于每条 user 消息最前，保住 DeepSeek 前缀缓存收益（v9 设计意图不变）；
- 总纲（build_intro_messages）改收：档案 + 十年主题预告 + 主角级事件年表（10 年窗口内），不再全量 455 条；
- 各篇文章的叙事引导（SECTION_REQ 增补）以十年主题为核心正向表述，如「本篇以本十年囚狱为主线，写出每次囚禁的由头、去向与后果」。

---

## 四、可行性验证（原型跑分，facts.py 真跑）

对 `player_38696.json + melt_908_01_01.json` 以 `as_of=908.1.1` 真跑 `F.build_facts`：

### 4.1 模块映射覆盖率

```
57 个 MEMORY_TEMPLATES 键 + death = 58 型 → 26 个时间线模块（拆到 30 含专题模块）
覆盖 58/58，缺失 (无)          ← 零未分类，生产环境不会出现「模块外事件」
```

### 4.2 每十年模块激活分布（原型实测）

| 十年 | 事件数 | 激活模块数 | Top5 模块 |
|---|---|---|---|
| 868–878 | 191 | 17 | 囚禁入狱 36 / 开战兴兵 34 / 添丁进口 27 / 起家发迹 27 / 战和胜负 16 |
| 878–888 | 71 | 11 | 丧亲之恸 16 / 添丁进口 13 / 囚禁入狱 10 / 教化求学 7 / 战和胜负 7 |
| 888–898 | 69 | 10 | 丧亲之恸 17 / 教化求学 11 / 囚禁入狱 10 / 添丁进口 9 / 结友知交 8 |
| 898–908 | 107 | 15 | 囚禁入狱 26 / 丧亲之恸 18 / 教化求学 16 / 添丁进口 10 / 开战兴兵 6 |

十年间模块分布差异显著（第 1 十年=征伐起家，第 2/3 十年=家室教化，第 4 十年=囚狱），证明「按十年主题切片」有真实区分度，不是空转。

### 4.3 切片后请求尺寸投影（原型）

以 第 4 个十年 为例（profile≈1.6K + 模块事件 + 主角级脊柱，去重）：

| 请求 | 现尺寸（实测） | 切片后（原型投影） |
|---|---|---|
| 总纲 | 13,011 | ≈2,554 |
| 本纪（lead+mid） | 26,827 / 26,622 | ≈3,041 |
| 家室列传 | 23,818 / 23,950 | ≈2,781 |
| 朝局风云录 | 20,911 / 21,202 | ≈2,818 |
| 列传·好友 | 16,019 / 15,976 | ≈2,068 |
| 列传·仇人 | 16,207 / 16,279 | ≈1,984 |
| 刺客列传（豁免整块，按 7.3 新口径） | 16,976 / 20,690 / 22,718 | ≈2,164（窗口19人×婚恋+丧偶） |
| **十年合计** | **313,432（16 请求）** | **≈35,602（7 请求）** |

**节约 ≈ 88.6%**（313,432 → 35,602）；刺客列传按新口径（十年窗口 + 亲缘/婚恋切片）后仅占切片后总量约 6%，豁免成本从 57% 降到一个零头（实测见 7.3）。

---

## 五、词元消耗账（对生成侧的实际意义）

1. **输入侧**：DeepSeek 按 token 计费，输入 token 占比大；−88% 输入 = 生成一篇十年小传的 API 成本降至约 1/8。
2. **延迟侧**：16 请求 → 7 请求（本纪/好友/仇人/家室/朝局 lead+mid 合并为每篇 1 个切片请求？否——lead/mid 仍分开，但每篇只调 2 次而非携带全量）；并发窗口内单请求 2–4.5K 字符，P90 从 27K 降到 ~4K，超时与重试概率同步下降。
3. **上下文长度安全**：40 年统治的第 4 个十年现 43K 字符上限（config max_tokens 12800），接近部分模型上下文红线；切片后单请求 ≤5K，统治 60–80 年也不会触顶。
4. **前缀缓存保留**：共享前缀（传主+档案+主题预告+主角级摘要）仍逐字节一致，跨请求命中 DeepSeek 上下文缓存；模块切片只发生在「相关事实」段，不动前缀。

---

## 六、叙事效果增强分析

### 6.1 注意力收拢（问题 4 的机制性修复）

现方案 455 条年表里 89% 与主角无关，模型注意力被 405 条宗族噪音稀释——这是「上下文多、戏剧性弱」的直接机制（问题 4 已量化）。模块切片后：
- 本纪只看权力/兵戈/刑狱模块 + 主角级事件：**第 4 个十年从 107 条收到 43+13 条**，主角的囚狱主线（26 条）与起家发迹（2 条）不再被 26 条 `relative_died` 淹没；
- 家室列传只看婚恋/生育/丧亡模块：从 107 条收到 29 条，门庭恩怨主线清晰；
- 模型拿到的每一条都是「本篇该写的事」，叙事引导（SECTION_REQ 主题化）才有落点。

### 6.2 戏剧性事实高亮的承接

facts.py v13 的 `dramatic_facts`（一日皇帝/短命皇朝）已证明「数据驱动高亮 + 独立块」有效（菲利普3 问题 2 修复）。本研究把同一思路从「单条高亮」扩展为「整十年主题化」：十年主题预告 = 总纲的戏剧承诺，各篇围绕承诺取材，形成「预告→展开→收束」的戏剧结构。

### 6.3 一致性锚点

- 总纲携带十年主题预告（Top2–3 模块），各篇并发生成时以同一预告为锚，避免「本纪写囚狱、家室写教化」各说各话；
- 主角级事件（50 条）进入所有相关文章，保证跨文章的主角行迹不矛盾。

---

## 七、风险与对策

### 7.1 信息缺失（跨模块关联事件被裁）

风险：同一事件可能同时属多个主题（如「囚禁→处决→结仇」链条跨 囚禁入狱/死亡谢幕/结仇结怨 三模块），只发一个模块会断链。
对策：
- 主角级事件（主角名在文本中）无条件进入所有相关文章（数量小：50/455）；
- 文章→模块映射允许重叠（战争/囚禁同时进 本纪+朝局），链条事件必然双侧可见；
- 主题检测的 Top3 之外，与 Top1 同源模块（如囚禁+获释+刑虐）合并为「主题簇」整体入切片；
- 切片尾部附「本十年另有 N 条宗族婚丧琐事从略」合计行（问题 4 修复 1 的级别 3 口径）。

### 7.2 并发文章口径不一致

风险：7–16 个请求并发，各文章素材不同，可能对同一事件叙述矛盾。
对策：共享前缀带十年主题预告；SECTION_REQ 按主题正向表述（不写「不要偏离主题」，写「本篇以X为主线」）；组装后人工抽查对照（回归时检查同一日期事件跨文章一致）。

### 7.3 刺客列传豁免成本 —— 按「官职名+亲缘+婚恋」压缩口径（已实施，v14）

用户定稿：刺客列传只传输受害人**父母、配偶、结婚/离婚相关记忆**，死者名带官职（如「唐皇帝李漼」）；**十年窗口切片不做**（原方案撤回）。实测（as_of=908.1.1，46 死者）：

| 方案 | 内容 | 字符 | ≈tokens（0.7~1.0/字） | 对比当前 |
|---|---|---|---|---|
| 当前格式（v13 全量） | 门第+官职+生于+死句+为人+**全部生前经历**（324 条记忆） | 12,808 | ≈9.0K–12.8K | 基准 |
| 新口径·已实施 | 官职+名（唐皇帝李漼）+ 死句 + 亲缘（父/母/妻/妾）+ 婚恋事件 | 4,309 | ≈3.0K–4.3K | **−66%** |

- **婚恋口径裁 events**：324 条死者记忆里婚恋类仅 **30 条（9%）**——married×23、spouse_died×4、became_lovers×2、broke_up_lovers×1（divorced 本数据集 0 条）；被裁的 294 条（relative_died×77、ascended_throne×36、child_born×35、became_friends×20、torturer×16…）多数与「刀下之魂」叙事无关。
- 实施细节（biography.py `_assassin_kill_lines`）：死者名 = `office + name`（唐皇帝李漼）；亲缘行从缓存 family 取父/母/妻/妾（按 label+名去重）；events 只保留含 成婚/相恋/私情/分手/丧偶/离婚 的婚恋记忆。
- 注意：新口径下每名死者新增「亲缘」行（父/母/妻/妾），是当前格式没有的**信息增量**（模型由此可知死者身后家室），不是纯削减。

### 7.4 主题检测稳定性

风险：模块计数受事件密度干扰（第 1 十年 191 条 vs 第 2 十年 71 条），Top-N 可能被噪音挤占。
对策：权重按 主角参与×3 > 直系×1（时间线已只含直系，级别3剔除后无路人）；**Top10 + 并列第10名全保留**（用户定稿，`decade_module_top` 实测：第 4 个十年恰好 10 个，边界测试 11 个时并列第10名全收）；阈值由「至少 10 个主题」天然保证。

### 7.5 回归验证面

十年小传输出篇幅/风格将有可见变化（预期效果，问题 4 待确认项 5 已预告）；需 `--force` 重跑 菲利普2 全部 4 篇十年小传对照。

---

## 八、与既有修复方案的关系

| 修复方案_菲利普2.md 问题 4 修复项 | 本研究关系 |
|---|---|
| 1. 年表分级限量（终传 ≤150、十年 ≤80 条） | **前置依赖**：模块切片先分级，限量自动达成（十年窗口 69–191 条 → 文章切片 2–43 条） |
| 2. 共享块瘦身（年表改为按文章取） | **本研究的直接延伸**：不止按文章取，还按模块取 |
| 3. 戏剧性事件高亮扩展 | 复用 `dramatic_facts` 渲染器，升级为十年主题 |
| 4. 各篇叙事引导（戏剧点） | 与 SECTION_REQ 主题化合并实施 |
| 5. 十年传记按阶段切片（仿 assassins mid1/2/3） | 替代方案：按模块切片更贴合「戏剧性」目标；两案可叠加（模块内再按时段） |

实施顺序完全兼容：**先做问题 4 第 1–2 条（年表分级 + 共享块瘦身），本研究第 3–5 步在其上叠加**。

---

## 九、实施记录（v14 已完成）

| 序 | 改动 | 文件 | 验证 |
|---|---|---|---|
| 1 | 问题1：determine_folder 改 max+1（无编号 base 目录计 1） | pipeline.py | 4 组边界测试通过：空→菲利普；有菲利普→菲利普2；菲利普+菲利普2→菲利普3；菲利普+菲利普3→菲利普4 |
| 2 | 问题2：watch 换玩家时先查同 playthrough 续用文件夹；后台线程 generate_bio 传 continue_mode=True；绑定只写不覆盖 | pipeline.py | 模拟父死子继（PT1）续用菲利普、新局（PT2）新建菲利普2 通过 |
| 3 | 问题3：`_office_word` 独立天朝制改游戏真实键（king_male_chinese=王/女王）；house_feuds 按事件日期重渲染两端角色（`_feud_role_title`/`_rerender_feud_event`） | facts.py | 实测「粤王范承宗」「瑞典国王崔佛·菲利普」按日期正确；901 处决唐氏、872 松恩酋长劫掠威塞克斯公爵均补全国号 |
| 4 | 问题4a：`_related_ids` 改分级 dict，级别3（其余宗族/姻亲）全部剔除；`_timeline` 限量（十年 ≤80、终传 ≤150，超限保主角级） | facts.py | 十年 455→80 条（−82%），终传 150 条 |
| 5 | 问题4b：`_shared_facts_block` 瘦身为 传主+档案+主角级摘要（50条/1.3K），全量年表按文章取 | biography.py | 十年总字符 313,432 → 100,482（−68%） |
| 6 | 问题4c：`dramatic_facts` 扩展（短命皇朝+主角级人生转折点，上限12）；SECTION_REQ 各篇补戏剧引导（正向表述） | facts.py、biography.py | 12 条转折点：登位/开战/囚禁/结仇/战争 |
| 7 | 30模块：MODULE_TABLE（26 时间线模块+4 专题）+ `_timeline` 事件标注 module（death 按死者关系并入 仇雠消亡/丧友/丧偶/丧亲） | facts.py | 80/80 全标注，零未分类 |
| 8 | 十年主题：`decade_module_top` Top10 并列第10名全保留 + 总纲/各篇接入主题预告（十年称「本十年」、终传称「一生」） | facts.py、biography.py | 第4十年 10 主题；边界测试 11 个并列全收 |
| 9 | 刺客列传：死者名带官职（唐皇帝李漼）+ 亲缘（父/母/妻/妾）+ 婚恋记忆口径（不做十年窗口切片，按用户指令撤回） | biography.py | mid 切片实测「唐皇帝李漼（殁于…被处决…）」+亲缘+婚恋 |

**全链路验证**：`py_compile` 4 文件 OK；模块导入 OK；mock-LLM 跑 `generate_biography(decade=4, as_of=908)` 16 次调用组装无异常。

## 十、待确认项（v14 已定稿/遗留）

1. **每十年主题数**：已定稿 **Top10 + 并列第10名全保留**（用户指令）。
2. **刺客列传口径**：已定稿 **官职名+亲缘+婚恋**，十年窗口切片不做（用户指令）；「同家族合并」实测反效果已废弃。
3. **年表级别3**：已定稿 **全部剔除**（用户指令），不做合计行。
4. **文件夹编号**：已定稿 **max+1**（用户指令），数据回迁不做（用户新开档测试）。
5. **国号层级词**：已定稿 **游戏口径「桂王」**（`king_male_chinese`=王，不追加「国」层级词；`kingdom_celestial_chinese_independent`=国 仅用于头衔显示如「青徐国」）。
6. **遗留**：`--force` 重跑 菲利普2 4 篇十年小传 + 终传，对照 prompts.log 校验请求数/字符量/主题命中/正文戏剧性；抽查跨文章日期一致性（用户新开档后执行）。

---

*附：原型脚本与数据源*
- prompts.log 解析：`logs/prompts.log`（21:34 / 23:16 两场十年小传全部请求的字符解剖）；
- 原型跑分：`output/菲利普2/data/player_38696.json` + `melt_908_01_01.json`，`F.build_facts(as_of="908.1.1")`；
- 关键代码锚点：`biography.py:698`（共享块）、`biography.py:482`（_article_facts）、`facts.py:1867`（_timeline）、`facts.py:92`（MEMORY_TEMPLATES）、`facts.py:1024`（dramatic_facts）、`pipeline.py:591`（_decade_cutoff）。
