# 菲利普2 测试集问题修复方案（审阅稿）

> 排查基准：`output/菲利普2`（868–911 年，玩家 酋长崔佛 id=38696 = 崔佛·菲利普(846)，44 份年度快照，已出 4 篇十年传记 + 911 终传）+ `output/菲利普3/菲利普4/菲利普5`，日志 `logs/journal.log` / `logs/prompts.log`，代码 `pipeline.py / facts.py / biography.py / cache_lib.py`。
> 每条结论带日志行号或代码行号作证据。新增提示词遵循「正向表述」铁律（no-negative-prompts），文件一律 UTF-8。

---

## 问题 1：watch 新建了「菲利普2」，但当时最新文件夹已是「菲利普3」——应建「菲利普4」

### 现象

- `output/菲利普3` 创建于 **8/28 13:39**（日志 1029 行：`新会话文件夹: [菲利普3]`），
  `output/菲利普2` 创建于 **8/29 21:09**（日志 16857 行：`新会话文件夹: [菲利普2] (本次 watch 运行新建)`，随后并入 868.1.1 玩家 38696）。
- 即：新建 菲利普2 时，编号更大的 菲利普3 早已存在，会话编号出现「倒退」。

### 根因（编号取「最小可用」而非「最大+1」）

`determine_folder`（pipeline.py:302–310）：

```python
def determine_folder(name, output_dir):
    base = sanitize_folder_name(name)
    if os.path.exists(os.path.join(output_dir, base)):
        i = 2
        while os.path.exists(os.path.join(output_dir, f"{base}{i}")):
            i += 1
        return f"{base}{i}"
    return base
```

它从 2 开始找**第一个空缺**。当时磁盘上是 `菲利普`（8/27）、`菲利普3`（8/28）——`菲利普2` 不存在，于是直接返回 `菲利普2`，无视已存在的更高编号 `菲利普3`。

而 `find_latest_session_folder`（pipeline.py:313–327）和 `_cache_pick_key`（pipeline.py:224–225）都把「编号大 = 最新会话」作为稳定判据（注释自述「会话文件夹编号才是最新会话的稳定判据」）。编号倒退会破坏这个判据，也让阅读页/续传选错文件夹。

### 修复

把 `determine_folder` 改为「**已有编号的最大值 + 1**」语义：

```python
def determine_folder(name, output_dir):
    base = sanitize_folder_name(name)
    max_seq = 0
    if os.path.isdir(output_dir):
        for fn in os.listdir(output_dir):
            m = re.match(re.escape(base) + r"(\d+)$", fn)
            if m:
                max_seq = max(max_seq, int(m.group(1)))
    return f"{base}{max_seq + 1}" if max_seq else base
```

- 效果：`菲利普`、`菲利普3` 并存 → 新会话 = `菲利普4`（符合用户预期）；
- `find_latest_session_folder` / `_cache_pick_key` 无需改动，编号恢复单调。

---

## 问题 2：第一代统治者死后，程序突然新建了「菲利普4」（且前代终传被写进新文件夹「菲利普5」，缓存绑定被污染）

### 现象（日志 19939–19956，8/29 夜）

```
19939  并入 911.1.1: 玩家 酋长崔佛 (id=38696)          → 菲利普2（本战役文件夹）
19943  [23:27:14] 新会话文件夹: [菲利普4] (本次 watch 运行新建)   ← 儿子富兰克林继位时新建！
19944  并入 912.1.1: 玩家 国王富兰克林 (id=52146)       → 菲利普4
19945  [23:27:24] [检测] 前代玩家 38696 (崔佛) 殁于 911.5.19 — 待生成终传
19950  [23:27:40] 新会话文件夹: [菲利普5] (本次 watch 运行新建)   ← 前代终传又被写进新文件夹！
19954  [23:28:40] 传记已生成: 菲利普5\崔佛·菲利普(846)_终传_911_05_19.md
```

实测缓存（`菲利普2/data/player_38696.json`）：
- `playthrough_id = 0fa09a56-a2e4-4d82-bde0-74013d49ce7d`，与 52146（富兰克林）**完全相同**（父死子继、同一战役）；
- `output_folder = 菲利普5` —— **缓存绑定被终传生成逻辑污染**（数据在菲利普2，绑定却指向菲利普5）。

### 根因（watch 会话文件夹按 player_id 建，不按 playthrough_id 续用；后台传记线程复用 watch 语义）

1. **继位被当成「新局」**：`_WATCH_SESSION`（pipeline.py:333）以 `player_key = player_id` 为键，
   `resolve_output_folder` watch 分支（pipeline.py:360–370）在「player_key 变化」时一律 `determine_folder` 新建。
   父死子继是**同一 playthrough**（README 明确「同一战役（playthrough_id 相同，含父死子继）共享文件夹」），
   代码却因 player_id 改变而新建 菲利普4。

2. **后台终传线程又建一个文件夹**：`_bio_worker_loop`（pipeline.py:997–1046）调 `generate_bio` →
   `output_paths`（pipeline.py:568–588，`resolve_output_folder(cfg, cache, continue_mode=False)`）——
   在 watch 运行期间 `_WATCH_SESSION["active"]=True`，死去的 38696 与当前会话玩家 52146 键不同 → 再 `determine_folder` 建 `菲利普5`。

3. **缓存绑定被覆盖**：`generate_bio`（pipeline.py:643–647）：
   ```python
   if cache.get("output_folder") != house:
       cache["output_folder"] = house
       cl.save_cache(cache, find_cache_path(cfg, cache.get("player_id")))
   ```
   把 38696 的绑定从 `菲利普2` 改成 `菲利普5`，但其缓存文件与 44 份熔件都在 菲利普2。此后 `find_cache_path`/`_generated_decades_on_disk`/续传都会按错误绑定找文件夹。

### 修复

1. **watch 模式续用同战役文件夹**：`resolve_output_folder` watch 分支在 `player_key` 变化时，先查
   `all_caches(cfg)` 中是否有 `playthrough_id` 相同的缓存 → 有则沿用其 `output_folder`（父死子继共享文件夹），
   无才新建。逻辑与 continue 分支（pipeline.py:349–354）对齐。
2. **后台传记线程一律按「缓存绑定」解析输出文件夹**：`_bio_worker_loop` 调 `generate_bio` 时
   显式 `continue_mode=True`（或让 `output_paths` 在 `_WATCH_SESSION` 下对非当前玩家优先用 `cache["output_folder"]`），
   后台线程永不新建文件夹。
3. **`generate_bio` 只绑定、不覆盖**：`output_folder` 已存在且目录存在时不再改写；只有首次解析出文件夹时才写入绑定。
4. **数据回迁（一次性脚本）**：把 `菲利普5` 的终传 md 移回 `菲利普2`，`菲利普4/data` 的熔件+缓存并入 `菲利普2`（同一 playthrough），
   修正 38696 / 52146 的 `output_folder` 绑定，重建 `菲利普2/index.html`；空壳目录 菲利普4/菲利普5 删除或保留由用户定。

---

## 问题 3：「国王崔佛劫掠了王范承宗」缺国号 —— 应为「（瑞典国王）崔佛劫掠了（桂/粤）王范承宗」

### 现象

`logs/prompts.log` 24245 行（911 终传相关事实）：
```
903年9月22日，国王崔佛劫掠了王范承宗
```
玩家在游戏中此时显示「桂王/粤王范承宗」——国号（桂/粤，AUH 天朝制自选国号）丢失。

### 根因（存档 change_reason 本身就是「王」，未用我们的头衔数据补全）

- 该文本来自 `house_relations.database` 的 `change_reason`，是 **CK3 存档里预本地化的字符串**。
  melt_904 原文（`\x15ONCLICK:CHARACTER,38696 … \x15high 国王\x15!\x15high 崔佛\x15!…劫掠了
  \x15ONCLICK:CHARACTER,60344 … \x15high 王\x15!，\x15high 范\x15!\x15high 承宗\x15!`）——
  游戏自己在 change_reason 里就只写「国王/王」，**不含国号**。
- `house_feuds`（facts.py:1134–1190）把 `change_reason` 原文经 `_clean_ck3_loc`（facts.py:1761–1771）清洗后直出，
  没有用我们自己的头衔机制补国号。
- 但**国号数据在存档里是齐的**：k_lingxi 的 `title_name_data.title_history_names`：
  `867.1.1 k_lingxi → 882.2.10 dynn_title_gui(桂) → 890.7.31 dynn_title_yue_2(粤)`；
  903.9.22 时 60344 首要头衔 = k_lingxi（kingdom），`_name_at_date` 得「粤」，`official_title` 得「粤国王」（本地化 `kingdom_celestial_chinese_independent=国` + 王）。
  主角 38696 在 903 时首要头衔 = k_sweden，`official_title` = 「瑞典国王」。
- 即：**渲染层有正确头衔能力，但恩怨史文本绕过了它，直接透传了游戏的不完整字符串**。

### 修复

1. `house_feuds` 渲染事件时，把 change_reason 中的两个角色（ONCLICK:CHARACTER,id 给出 id，事件自带日期）重新渲染：
   - 主角侧（或任意带 id 角色）：`official_title(cid)` 或 `_primary_title_at(cid, date)+国号` → 「瑞典国王崔佛」；
   - 对方侧：`_title_name_at(tid, date) + 层级词 + 姓名` → 「粤国王范承宗」（与 `_titled_name_at` 渲染口径一致）。
   - 模板保留游戏动词（劫掠了/囚禁了/处决了/成为朋友…），只替换两端的「称号+名」。
2. 事件日期已知（`e.get("date")`），用 `as_of` 之外的日期版头衔查询（`_primary_title_at(cid, date)` / `_title_name_at(tid, date)`），
   保证国号随年份正确（903 是粤、更早是桂）。
3. 顺带：`_clean_ck3_loc` 现有的「删称号与名字间逗号」逻辑保留；重渲染后自然产出「粤国王范承宗」式连贯名。
4. 验证口径与游戏一致：官方 UI 显示「桂王/粤王」级别词，与我们 `official_title`（粤国王）若不一致，以游戏为准微调层级词。

---

## 问题 4：上下文比 D:\Journal 多得多，戏剧性却弱得多

### 量化对比（prompts.log 实测）

| 项目 | 请求数 | 中位 | P90 | 最大 |
|---|---|---|---|---|
| 本项目 Roman | 80 | **13,065 字符** | 27,130 | 43,489 |
| D:\Journal | 866 | 2,236 | 3,233 | 10,886 |

911 终传单条请求解剖：
- 总 13.9K 字符，其中【主角大事年表】占 **12.4K / 487 行**；
- 487 条里只有 **53 条**与主角本人（崔佛·菲利普）相关、42 条与族亲（崔佛XX氏）相关，**392 条完全无关**
  （李漼登位得醴泉县、大和房世与柔子成婚… 全宗族+姻亲的记忆都进了年表）。

### 根因

1. **年表过滤口径过宽**：`_related_ids`（facts.py:1803–1864）含 ① 全宗族（同 dynasty_id 所有人）② 父母妻妾子女
   ③ 孙辈+儿媳女婿 → 483 条候选事件里 80% 是远亲/姻亲的琐事，主角自己的戏剧（903 劫掠范承宗、899 得瑞典、901 处决唐氏）被淹没。
2. **共享块全量重发**：`_shared_facts_block`（biography.py:698–720）把「传主+档案+**全部年表**」注入每一次调用
   （总纲/五篇开篇/各篇中段共 16 次），每次 13–43K。模型注意力被 392 条噪声稀释。
3. **缺少戏剧引导**：D:\Journal 每板块有明确叙事要求（SECTION_DEFS，如「每期必须至少有一条以（头衔）（统治者姓名）……为主干的统治者活动新闻」「与民生访谈形成贫富对照」），
   且按板块只传该板块数据；本项目 SECTION_REQ 较平（「以资料为限」），无「本篇必须呈现的戏剧点」。
4. D:\Journal 的优势：按板块切片事实 + 年度差分（去年 vs 今年）+ 风格化 voice + 明确的叙事装置。

### 修复（分层，可独立取舍）

1. **年表分级与限量**（facts.py `_timeline` / `_related_ids`）：
   - 级别 1：主角本人事件（必收）；级别 2：父母/妻妾/子女/孙辈；级别 3：其余宗族/姻亲（默认不收或压缩为一行合计）。
   - 上限：终传 ≤ 120–150 条、十年传记 ≤ 60–80 条；超限按级别截断，尾部给「其余 N 条从略」。
2. **共享块瘦身**（biography.py `_shared_facts_block`）：
   - 共享前缀只留【传主】【人物档案】；**大事年表改为按文章取**（`_article_facts` 已按 key 取块，把年表切片下放到各篇，
     本纪=主角级事件、家室=家室事件、朝局=政治事件、恩怨录=恩怨史，互不重发全量）。
3. **戏剧性事实高亮**（facts.py 已有 v13「戏剧性事件」= 短命皇朝）：
   - 扩展为数据驱动的「人生转折点」：登位/失土/被囚/处决/结仇/丧子/加冕等按「主角参与 + 事件类型」加权，
     渲染为【戏剧性事件】独立块放在人物档案之后（正向表述，如「写出此段戏剧性事件」）。
4. **各篇叙事要求补戏剧引导**（biography.py SECTION_REQ）：
   - 仿 D:\Journal：每篇加一条「本篇须写出的核心场景/转折」（如 本纪=至少一处登位/失土时刻；恩怨录=把劫掠→囚禁→处决的链条按日期写全；
     朝局=帝位/最高领主更替；家室=妻妾子女聚散）。
   - 全部正向表述；具体措辞写入后在 prompts.log 自查一遍。
5. **十年传记/终传按阶段切片**：中段板块可仿 assassins 的 mid1/mid2/mid3（biography.py:74–79）按年代切片，
   降低单次调用上下文、提升每段叙事密度。

---

## 实施顺序建议

| 序 | 改动 | 涉及文件 | 风险 |
|---|---|---|---|
| 1 | determine_folder 改 max+1 | pipeline.py | 低 |
| 2 | watch 继位续用同战役文件夹 + 后台线程按绑定解析 + generate_bio 不覆盖绑定 | pipeline.py | 中（动会话核心，需回归 watch/continue 全流程） |
| 3 | 数据回迁脚本（菲利普4/5 → 菲利普2、修正绑定、重建 index） | 一次性脚本 | 中（先备份） |
| 4 | 恩怨史国号补全（按日期头衔重渲染两端角色名） | facts.py | 中 |
| 5 | 年表分级限量 + 共享块瘦身 | facts.py、biography.py | 中 |
| 6 | 戏剧性事件扩展 + 各篇叙事要求 | facts.py、biography.py | 低（纯提示词/渲染层） |
| 7 | 用 菲利普2 全量重跑回归（4 篇十年 + 终传 + 父子继位续跑），对照 prompts.log 校验提示词与文件夹行为 | — | — |

每步改完跑 `python pipeline.py status` 与单篇 `bio` 验证；新增提示词字符串过一遍正向表述自查清单。

## 待确认项

1. 文件夹编号语义：确认采用「max+1」（菲利普3 之后建 菲利普4），废弃「最小空号」？
2. 国号层级词口径：游戏显示「桂王/粤王」还是「桂国王/粤国王」？（本地化表 `kingdom_celestial_chinese_independent=国`，官方中文习惯上我方目前渲染为「粤国王」；若游戏为「粤王」则调层级词。）
3. 年表级别 3（远亲/姻亲）事件：全部剔除，还是压缩为「本年另有 N 起宗族姻亲琐事」合计行？
4. 回迁后 菲利普4/菲利普5 空目录：删除还是保留留档？
5. 终传 16 次调用是否都接受瘦身后变化（输出篇幅/风格可能有可见变化，属预期效果）。
