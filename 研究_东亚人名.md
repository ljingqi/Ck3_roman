# 研究：AUH 东亚人名正常化方案（宗族/家族分层）

> 研究对象：《十字军之王3》(CK3) 官方 DLC「All Under Heaven」(AUH) 加入的东亚人名在
> 本项目（《CK3 家传》）导出中的呈现问题。本文基于游戏文件
> `F:\Game\steamapps\common\Crusader Kings III\game\`（含全部 DLC 合并内容，RICE 等 Mod 已启用）
> 与项目自身数据（`data/localization.json`、各战役 melt、最新自动存档 912.1.1 熔化件）逐项验证。
> 结论先行：**问题根因是「项目把 CK3 的『家族(house)名』当姓用，而游戏对东亚文化显示的是
> 『宗族(dynasty)名』」；修复分数据层（解析游戏 dynasty/dynasty_houses 定义文件）与
> 显示层（东方名序改用宗族名作姓）两步，风味层再把宗族+家族信息都传给模型。**

---

## 1. 问题复述

- 西方人名用 CK3 的**家族（house）名**作姓（冯·大马士革、欧特维尔），项目现状正确。
- AUH 加入东亚人名后出现两类错位：
  1. **中国**：宗族恒为姓（李/金），但自动生成的**家族名**会带地名前缀（交州金）。
     游戏内人物始终显示宗族姓（李XX、金XX），项目却可能输出「交州金XX」。
  2. **日本**：宗族（氏）名作姓显示（大和、藤原），家族（家）表示分家（北家、一条……）。
     项目直接输出「家族名+名」→「一条XX」「北家XX」，而游戏显示「藤原XX」。
- 目标：让导出人名与游戏内显示一致（正常化），同时在传给模型的文本中保留宗族/家族
  两层信息以维持风味。

---

## 2. 游戏机制研究结论（含证据）

### 2.1 概念层：宗族 = dynasty，家族 = house

CK3 简体中文官方本地化：

```
game_concepts_l_simp_chinese.yml:435  game_concept_house: "家族"
core_l_simp_chinese.yml:443           DYNASTY: "宗族"
```

即用户口径与游戏口径一致：**宗族=DYNASTY，家族=HOUSE**（README 中「游戏内新建家族（支系）
不改变宗族」同义）。每个角色同时挂在宗族与家族两层下，家族是宗族的分支（创始家 = 主家）。

### 2.2 显示规则：东亚姓 = 宗族名，西方姓 = 家族名

`localization/simp_chinese/character_l_simp_chinese.yml` 的姓名模板（英文同源）：

| 名序约定 | 模板键 | 模板 |
| --- | --- | --- |
| 默认（西方） | `CHARACTER_FIRST_NAME_AND_DYNASTY` | `$NAME$·$HOUSE$`（名·**家族名**） |
| `dynasty_always_first`（汉/韩/越） | `..._DYNASTY_ALWAYS_FIRST` | `$DYNASTY$$NAME$`（**宗族名**+名） |
| `japanese`（日） | `..._JAPANESE` | `$DYNASTY$$NAME$`（**宗族名**+名） |

即：**东亚文化的姓 = 宗族（dynasty）名，不是家族（house）名**；家族名只在家族窗口显示
（`HOUSE_VIEW_NAME_WITH_HOUSE: "[DynastyHouse.GetBaseNameNoTooltip]家族"`）。

游戏内名序约定实测（912.1.1 存档 `culture_manager`）：`DYNASTY_ALWAYS_FIRST` 40 个文化、
`JAPANESE` 4 个，与项目 `EASTERN_NAME_ORDERS = {"DYNASTY_ALWAYS_FIRST", "JAPANESE"}` 完全对应。

### 2.3 数据层：宗族 key 带「地望/本贯」，宗族名恒为纯姓

`common/dynasties/05_tgp_dynasties.txt` 中东亚宗族定义的关键特征——**key 常含地名前缀，
但 `name` 永远是纯姓**：

```
korean_choe_gyeongju = { name = "dynn_Choe_5D14" # 崔 }   ; key=慶州崔
korean_gim_silla     = { name = "dynn_Gim_..."     # 金 }  ; key=慶州金
tribal_yanzhou_luo   = { name = "dynn_Luo_7F85"    # 罗 }  ; key=兖州罗
japanese_minamoto_seiwa = { name = "dynn_Minamoto" # 源 }  ; key=清和源氏
japanese_fujiwara    = { name = "dynn_Fujiwara"    # 藤原 }
han_imperial_liu     = { name = "dynn_Liu_5289"    # 刘 }  ; 汉朝刘氏
```

**家族（分家）**定义在独立的 `common/dynasty_houses/`：

```
tgp_dynasty_houses.txt:
house_fujiwara_konoe    = { name = "dynn_Fujiwara_Konoe"  # 近衛  dynasty = japanese_fujiwara }
house_fujiwara_kujo     = { name = "dynn_Fujiwara_KujO_"   # 九条  dynasty = japanese_fujiwara }
house_fujiwara_nan_ke   = { name = "dynn_Fujiwara_Nan_ke"  # 南家  dynasty = japanese_fujiwara }
```

日本新建分家的取名机制（`10_dlc_tgp_japan_scripted_effects.txt:2414`
`assign_japanese_house_name_options_kyoto_effect`）：京都街道名名单（一条/二条/近衛/北畠…）
或领地地名（`japanese_house_random_barony` / `primary_county` 等）——即用户所说「家族表示分家」。
日本名字表 `00_japanese.txt` 亦带 `house_based_map_names = yes`。

**存档实测（912.1.1 熔化件 `dynasties` 段）**，家族/宗族的存储形态：

```
house 5408 → {localized_name: 北家, dynasty: 5408}            ; 藤原主家（创始家名=北家！）
house 11022 → {key: house_fujiwara_kajuji, dynasty: 5408}      ; 勧修寺（需查 game 文件）
house 5493  → {name: dynn_Soga, dynasty: 5493}                 ; 蘇我（创始家=宗族同名）
house 5247  → {localized_name: 庆州崔, dynasty: 5247}          ; 韩国本贯式家族名
house 5270  → {localized_name: 庆州金, dynasty: 5270}
house 16789292 → {localized_name: 太田, dynasty: 5408}         ; 游戏生成分家
dynasty 5408 → {key: japanese_fujiwara, ...}                   ; 注意：存档不存宗族显示名！
dynasty 5247 → {key: korean_choe_gyeongju, ...}
```

三个要点：
1. **存档中宗族只有 key，没有显示名**（`name`/`localized_name` 缺失）——游戏的宗族显示名
   （藤原/崔/金）由 key 回查 `common/dynasties/*.txt` 的 `name = "dynn_X"` 再本地化得到。
2. 家族名三种形态：存档 `localized_name`（北家/庆州崔/太田）、存档 `name`（dynn_Soga）、
   存档 `key`（house_fujiwara_kajuji → 查 `common/dynasty_houses/*.txt`）。
3. 实测 7355 个 CJK 家族中 **385 个「家族名 ≠ 宗族名」**（韩国本贯式：庆州崔/庆州金/全州李/
   安东权/坡平廉…；日本：藤原主家=北家、诸分家）。这些家族的角色若按家族名渲染，
   姓就全错了。边氏战役（869）玩家「边诚」恰好创始家名=宗族名（边=边）所以现状碰巧正确。

### 2.4 小结：游戏内的三层名字

| 概念 | CK3 层 | 中国示例 | 日本示例 | 韩国示例 |
| --- | --- | --- | --- | --- |
| 姓（角色显示） | **dynasty 名**（`name`→dynn_ 本地化） | 李/金 | 藤原/源/大和 | 崔/金/李 |
| 宗族全称（含地望） | dynasty **key** | 交州金（新贵生成） | 清和源氏 | 慶州崔 |
| 家族/分家 | **house**（localized_name/name/key） | 交州金 | 北家/一条/近衛 | 庆州崔/全州李 |

角色名 = 宗族名 + 名；家族名只出现在家族界面与「XX家族」等语境。

---

## 3. 项目现状与问题定位（代码）

| 位置 | 现状 | 问题 |
| --- | --- | --- |
| `cache_lib.py:825 display_name` | 东方名序 `h + nm`，`h = house_name` | 姓取**家族名** → 庆州崔XX / 北家XX / 一条XX |
| `cache_lib.py:261 dynasty_name_zh` | ①localized_name ②key→本地化表 ③创始家兜底 | 存档无宗族名，②查不到（japanese_fujiwara 不在本地化表），③兜底取创始家名=北家/庆州崔 → **错** |
| `cache_lib.py:210 house_name_zh` | ①localized_name ②name→表/码点 ③house key→dynn_ 表 | 家族 `key`（house_fujiwara_kajuji）无 `common/dynasty_houses` 表可查 → 空 |
| `facts.py:1402 dynasty_name` / `2252,2500…` 档案 `house` 字段 | 全基于 house_name | 穆斯林国名（图伦苏丹国）与档案「家族：」行同样偏家族名 |
| `build_names.py` / `char_record` | 每角色只存 house_name | 缺 dynasty_name 字段 |
| `pipeline.py:291` 宗族文件夹名 | `dynasty_name or house_name` | 已优先宗族名，方向正确，但 dynasty_name 解析是错的（见上） |

一句话：**项目把「家族名」当姓用；游戏对东亚显示「宗族名」。且宗族名无法从存档单独
解析出来，必须补读游戏定义文件。**

---

## 4. 解决方案设计

### 4.1 数据层：解析游戏 dynasty / dynasty_houses 定义（localization.py）

仿照现有 `build_province_map`（已解析 `common/landed_titles`，含 Mod 覆盖）新增：

- `build_dynasty_table(cfg)` → 落盘 `data/dynasties.json`：
  - 遍历 `game/common/dynasties/*.txt` + 启用 Mod 同路径（`enabled_mod_dirs` 已支持，
    RICE 等 Mod 的宗族/家族定义一并进入），提取 `key = { name = "dynn_X" }`。
  - 遍历 `game/common/dynasty_houses/*.txt`，提取 `house_<key> = { name = "dynn_Y" dynasty = ... }`。
- `dynasty_name_of_key(key)` / `house_name_of_key(key)`：
  key → `dynn_X` → `localization.table()` 查中文（dynn_Li_674E → 李，与现有
  `decode_codepoints` 兼容），表缺则码点解码兜底。
- 原型已验证：6102 条宗族定义解析后 `japanese_fujiwara→藤原`、`korean_choe_gyeongju→崔`、
  `korean_gim_silla→金`、`tribal_yanzhou_luo→罗`、`han_imperial_liu→刘`，全对。

### 4.2 显示层：东方名序用宗族名作姓（cache_lib.py）

- `dynasty_name_zh(melt, dynasty_id)` 取值链插入新步骤（在创始家兜底**之前**）：
  `key → 宗族表 → dynn_ 本地化`。若查不到再走现有兜底（保持旧行为不回归）。
- `house_name_zh(melt, house_id)` 插入 `house key → 家族表` 步骤（补 勧修寺 这类）。
- `display_name(cache, cid, ...)`：`order in EASTERN_NAME_ORDERS` 分支中，姓改为
  **宗族名**（`dynasty_id_of` + `dynasty_name_zh`），宗族名缺失时回退家族名（现状兜底，
  保证西方文化/未知文化行为不变）。
- `char_record`（extract_snapshot）增存 `dynasty_name`；`build_names.py` 的 names.json
  每角色增加 `dynasty_name` 字段（display_name 的 names_path 兜底同步）。

### 4.3 风味层：宗族+家族双层信息进提示词（facts.py / biography.py）

- 档案字段改造：
  - `dynasty`（宗族）：沿用 `house_display` 的「单字加氏」规则 → 藤原氏 / 边氏 / 崔氏；
  - `house`（家族）：北家 / 庆州崔 / 交州金（仅当与宗族不同时输出，相同则省略，避免冗余）；
  - 档案（主角 `_protagonist`、角色 `_character_profiles`、妻族/朝局等 `house` 字段出处
    facts.py:2252/2500/2788/2968）统一改为 宗族 + 家族 两字段。
- 提示词文本自然获得风味：显示名（藤原道真）+ 宗族（藤原氏）+ 家族（北家），模型即可写出
  「藤原北家」式称呼；中国「金氏·交州房」同理。仍符合「干净事实铁律」（全中文，无裸键）。
- `facts.py:dynasty_name`（穆斯林国名用）同步改走宗族名解析，与游戏「图伦苏丹国=宗族名」一致。
- `pipeline.py` 宗族文件夹名逻辑已正确（优先 dynasty_name），无需改。

### 4.4 回归与验证

1. 现有战役全量回归：边氏（869）、冯·大马士革（1067-1074）、菲利普（868-959）输出名不变
   （西方走家族名分支；边诚 创始家=宗族名 结果一致）。
2. 912.1.1 档对拍：随机抽 385 个「家族≠宗族」家族的角色，断言 `display_name` =
   宗族名+名（崔XX/藤原XX/金XX…）。
3. 日本分家场景（存档暂无 一条 分家实例，可用 house_fujiwara_kajuji 等定义验证 house_name_zh）。

---

## 5. 边界与风险

- **存档 localized_name 不是宗族名**：北家/庆州崔 是家族名，新解析切不可把它当宗族名
  （这正是当前 dynasty_name_zh 兜底链的错误来源）。
- 父名制文化（patronym_rules）优先于名序分支，不受影响。
- 文化缺失（死后清空）推断路径只定名序；宗族名由 `dynasty_house` 直读，不依赖文化，
  死者姓名也可正常化。
- Mod：RICE 等启用 Mod 的 dynasty/dynasty_house 文件会被解析（复用 enabled_mod_dirs）；
  Mod 内自定义的 `dynn_` 中文若缺本地化则码点解码兜底。
- 游戏内新贵/自建宗族（key 如 `tribal_jiaozhou_jin` 或 `zhao_8D99_1`）：key 未收录时
  走创始家兜底=纯姓（金/赵）→ 显示正确。
- 性能：表构建一次落盘（同 localization.json 模式），运行时惰性加载；display_name 已有
  每次 build_facts 的 `_name_cache`，无额外开销。

---

## 6. 建议实施顺序

1. `localization.py`：`build_dynasty_table` + `data/dynasties.json`（含 Mod）。
2. `cache_lib.py`：`dynasty_name_zh` / `house_name_zh` 接入新表；`display_name` 东方名序
   改宗族名；`char_record`/`build_names.py` 增 `dynasty_name`。
3. `facts.py`：档案 `house` 字段拆为 宗族+家族；`dynasty_name()` 改宗族解析。
4. 回归（现有战役）+ 912 档对拍。

> 附注：本报告对应的原型脚本 `cache/_proto_dyn.py`（游戏宗族定义解析 + 差异统计）与
> 912.1.1 熔化件 `cache/_auto2.json` 已留存，可直接作为第 1、4 步的验证基座。

---

## 附：实施中发现的关联缺口（官职地名兜底）

实施后重生成菲利普2 第4个十年小传时，`886年10月29日，县令贾瑾死…究竟是何处县官…史料不详`
暴露一个既有缺口：**存档会剪除 title history**（b_lantian 在 911 熔件中 history 为空、
realm_history 不跟踪男爵领），`official_title` 的官职地名（蓝田县令）取不到 —— 该缺口与
v14 改动无关（git stash 还原旧代码 + 同数据仍输出「县令」），但新生成时模型未把记忆句
「885年8月27日，贾瑾登位，得蓝田县」与官职相连，遂写「史料不详」。

修复：`facts.py _last_title_place` 在 title history 缺失时回退死者 `dead_data.domain`
（死时辖地，存档仍存），按官职层级精确匹配取地名 → 蓝田县令 稳定恢复（重生成验证通过）。
